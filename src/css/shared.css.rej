diff a/src/css/shared.css b/src/css/shared.css	(rejected hunks)
@@ -3,7 +3,7 @@
 
 /* ── Reset ── */
 *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
-html { scroll-behavior: smooth; font-size: 16px; }
+html { scroll-behavior: smooth; scroll-padding-top: 80px; font-size: 16px; }
 
 /* ── Body ── */
 body {
@@ -158,28 +157,43 @@ footer {
   color: var(--white-text-secondary);
   padding: var(--space-3xl) 2rem var(--space-xl);
   margin-top: 80px;
+  border-top: 2px solid;
+  border-image: linear-gradient(to right, var(--teal), var(--sand-deep)) 1;
 }
 .footer-inner {
   max-width: var(--content-width);
   margin: 0 auto;
   text-align: center;
 }
-.footer-inner p { font-size: var(--text-sm); line-height: 1.7; margin-bottom: 4px; }
+.footer-inner p { font-size: var(--text-sm); line-height: 1.7; margin-bottom: 8px; }
 .footer-inner a { color: var(--white-text-link); text-decoration: none; }
 .footer-inner a:hover { color: white; }
 .footer-links {
-  margin-top: var(--space-md);
-  padding-top: var(--space-md);
+  margin-top: var(--space-lg);
+  padding-top: var(--space-lg);
   border-top: 1px solid rgba(255,255,255,0.08);
   font-size: var(--text-xs);
 }
-.footer-links a { margin: 0 10px; }
+.footer-links a { margin: 0 12px; }
+
+/* ── Global Transitions for Interactive Elements ── */
+a, button, input, .module-card {
+  transition: all 0.2s ease;
+}
 
 /* ── Accessibility ── */
 *:focus-visible {
   outline: 2px solid var(--teal);
-  outline-offset: 2px;
-  border-radius: 4px;
+  outline-offset: 3px;
+  border-radius: var(--radius-sm);
+}
+/* Focus outline on dark backgrounds: use lighter variant for visibility (WCAG 2.4.7) */
+.hero *:focus-visible,
+.module-hero *:focus-visible,
+.takeaways *:focus-visible,
+footer *:focus-visible,
+.storage-banner *:focus-visible {
+  outline-color: var(--teal-light);
 }
 
 /* ── Storage Consent Banner ── */
@@ -259,10 +273,10 @@ footer {
   display: inline-flex;
   align-items: center;
   padding: 6px 16px;
-  border-radius: var(--radius-round);
+  border-radius: var(--radius-pill);
   font-size: 0.75rem;
   font-weight: 700;
-  letter-spacing: 0.08em;
+  letter-spacing: 0.06em;
   text-transform: uppercase;
   margin-bottom: 16px;
 }
@@ -343,8 +357,8 @@ footer {
   font-size: 0.75rem;
   font-weight: 600;
   text-transform: uppercase;
-  letter-spacing: 0.12em;
-  color: var(--teal);
+  letter-spacing: 0.06em;
+  color: var(--navy-mid);
   margin-bottom: 12px;
 }
 .section-title {
@@ -377,9 +391,9 @@ footer {
   color: var(--text);
   background: white;
   border: 1px solid var(--border-warm);
-  border-radius: var(--radius-lg);
+  border-radius: var(--radius-md);
   padding: 32px 28px;
-  transition: border-color 0.2s ease, box-shadow 0.2s ease;
+  transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease, border-left-width 0.2s ease;
   position: relative;
   display: flex;
   flex-direction: column;
@@ -387,6 +401,7 @@ footer {
 }
 .module-card:hover {
   box-shadow: var(--shadow-card);
+  transform: translateY(-2px);
 }
 
 /* Accent: left border + hover border + number color */
@@ -397,17 +412,17 @@ footer {
 .module-card[data-accent="green"]  { border-left-color: var(--green-phase); }
 .module-card[data-accent="teal"]   { border-left-color: var(--teal); }
 
-.module-card[data-accent="blue"]:hover   { border-color: var(--blue-phase); }
-.module-card[data-accent="amber"]:hover  { border-color: var(--amber-phase); }
-.module-card[data-accent="purple"]:hover { border-color: var(--purple-phase); }
-.module-card[data-accent="green"]:hover  { border-color: var(--green-phase); }
-.module-card[data-accent="teal"]:hover   { border-color: var(--teal); }
+.module-card[data-accent="blue"]:hover   { border-color: var(--blue-phase);   border-left-width: 4px; }
+.module-card[data-accent="amber"]:hover  { border-color: var(--amber-phase);  border-left-width: 4px; }
+.module-card[data-accent="purple"]:hover { border-color: var(--purple-phase); border-left-width: 4px; }
+.module-card[data-accent="green"]:hover  { border-color: var(--green-phase);  border-left-width: 4px; }
+.module-card[data-accent="teal"]:hover   { border-color: var(--teal);         border-left-width: 4px; }
 
 .module-number {
   font-size: 0.7rem;
   font-weight: 700;
   text-transform: uppercase;
-  letter-spacing: 0.14em;
+  letter-spacing: 0.06em;
 }
 .module-card[data-accent="blue"]   .module-number { color: var(--blue-phase); }
 .module-card[data-accent="amber"]  .module-number { color: var(--amber-phase); }
