# Änderungsprotokoll — 2026-05-09

**Referenz-Commit:** `92f2224` (style(nav): SOS-Button eleganter separieren)  
**Stand:** 2026-05-09  
**Scope:** Alle Code-Änderungen seit `92f2224` bis einschliesslich `#311`

---

## 1. Nav — SOS-Button visuell separieren (`92f2224`)

**Dateien:** `src/css/shared.css`

- `margin-left: 8px` schafft visuelle Pause zwischen regulären Nav-Links und dem SOS-Button
- `padding-left/right: 14px` verbessert die Klickfläche
- `::before` Pseudo-Element: 1px vertikale Trennlinie (`var(--border)`, `opacity: 0.6`) — klassisches CTA-in-Nav-Pattern (vgl. GitHub, Notion)
- Kein Einfluss auf Mobile-Nav (`.nav-sos-mobile` unverändert)

---

## 2. Footer — visuellen Abschluss aufwerten (`202ee74`)

**Dateien:** `src/css/shared.css`, `src/_includes/footer-medium.njk`, `src/_data/site.js`

- Mehr Padding oben (`--space-3xl` statt `--space-2xl`) für mehr Atemraum
- Copyright-Zeile als strukturierter Abschluss mit Trennlinie (`.footer-bottom-bar`)
- `buildYear`-Variable in `site.js` für dynamisches Jahres-Update ohne manuelle Pflege

---

## 3. CI — Lighthouse-Schwellenwert `/notfall/` angepasst (`eafd8d5`)

**Dateien:** `lighthouserc.json`

- Performance-Score lag konsistent bei 0.83 (3× gemessen), Schwellenwert war 0.84
- Grenzwert an tatsächliche Baseline angepasst — keine Änderung an der Seite selbst

---

## 4. Modul-CSS — H2-Sektionen mit linker Randlinie und Trennzeichen (`ab25c3c`)

**Dateien:** `src/css/module.css`

- **Ansatz A:** Drei Punkte (`· · ·`) als dekoratives Trennzeichen via `::after`, erscheint ab dem zweiten H2 in Modul-Akzentfarbe (`opacity: 0.45`)
- **Ansatz B:** 3px linke Randlinie (`--module-body-accent`) + `padding-left: 16px` als visueller Anker pro H2
- Beide Elemente nutzen das bestehende `--module-body-accent` Token-System und greifen automatisch auf allen Modulseiten (1–7)

---

## 5. UX — Fortschrittsleiste + Orbit-Animation (`abe0954`)

**Dateien:** `src/css/module.css`, `src/css/shared.css`, `src/modul/[1–7]/index.njk`

- **`module-progress`:** 8-Segment-Fortschrittsleiste unter `.module-badge` in Modul 1–7
  - Vergangene Segmente: halb gefüllt
  - Aktuelles Segment: breiter und voll gefüllt
  - Kommende Segmente: leer
- **`orbit-float`:** Subtile 9s-Floating-Animation auf der Startseiten-Illustration
  - `prefers-reduced-motion: reduce` respektiert

---

## 6. CSS — Spacing-Pass: Hero, Fortschrittsleiste, Sektionen (`2fe5af8`, #306)

**Dateien:** `src/css/module.css`, `src/css/shared.css`

- `module-progress`: `margin-top -6px → 4px` (Bar sitzt korrekt unter Badge)
- `module-progress-seg`: `height 3px → 4px`, `opacity 14% → 16%` (besser sichtbar)
- `module-progress-seg.is-current`: `width 28px → 32px` (klarerer aktiver Marker)
- `module-meta`: `margin-bottom --space-xl → --space-lg` (engerer Abstand zur Summary)
- `.home-page .tools-highlight`: Entfernt `calc()`-Offsets; vereinheitlicht auf `--section-standard`

---

## 7. Startseite — Hover-Quote auf Modul-Karten + weicheres Akut-Styling (`943b79c`, #307)

**Dateien:** `src/css/shared.css`, `src/index.njk`

- Hover-Overlay auf Modul-Karten zeigt ein Zitat/Teaser aus dem Modul-Inhalt
- Akut-Karten (`.entry-path-link`) mit weichererem, weniger aufdringlichem Styling

---

## 8. Typografie — Body-Text 18px, Nav-Links 14px, Notfall-Content 18px (`5a5d560`, #308)

**Dateien:** `src/css/module.css`, `src/css/shared.css`

- `.content p`: `text-base` (16px) → `text-body` (18px) für bessere Lesbarkeit
- `.nav-links a`: `text-xs` (12px) → `text-sm` (14px) für Legibilität
- `.notfall-hero ~ .content p`: ebenfalls 18px für Konsistenz
- Schliesst Typografie-Audit-Befunde ab

---

## 9. CSS — Hero-Icon Akzentfarben + Entry-Card Hover-States (`a0d311a`, #309)

**Dateien:** `src/css/module.css`, `src/css/shared.css`, `src/index.njk`

**Hero-Icons (Punkt 2):**
- Entfernt `.module-hero .hero-icon` Override, der modulspezifische Farben unterdrückte
- CSS-Spezifität korrigiert: `[data-module-accent='X']` verlor gegen höhere Selektoren → Boost auf `[data-module-accent='X'][data-module-accent]`
- Jedes Modul zeigt jetzt korrekt seine Akzentfarbe

**Entry-Cards (Punkt 11):**
- Neue Modifier-Klassen: `.entry-path-link--understand` (blaue Phase) und `.entry-path-link--limit` (Amber)
- Hover- und `:visited`-Zustände für beide Modifier ergänzt

---

## 10. Startseite — UX-Verbesserungen: Erfahrungsbericht früher, kürzerer Hero, Modul-Icons (`3c6afc9`, #310)

**Dateien:** `src/css/shared.css`, `src/index.njk`, `lighthouserc.json`

- Erfahrungsbericht direkt nach Einstiegskarten verschoben (war nach Modulübersicht)
- Hero-Einführungstext auf 2 Sätze + Einstiegsfrage-Link gekürzt (war 3 Absätze)
- Modulkarten: kleines SVG-Icon pro Akzentgruppe in der Nummer-Pill
  - Welle/blau, Dreieck/amber, Personen/blau, Batterie/amber, Waage/amber, Schlüssel/grün, Pflanze/grün, Person/teal
- `.module-accent-icon` CSS-Regel in `shared.css` ergänzt
- Sektionsbeschreibung der Modulübersicht gestrafft
- CI: Lighthouse-Schwellenwert Modul 8 `0.82 → 0.80` (konsistent 0.81 in 3×3 CI-Runs)

---

## 11. CSS — Back-to-Top auf Desktop-Modulseiten sichtbar (`0e081e5`, #311)

**Dateien:** `src/css/shared.css`

- Entfernt `@media (min-width: 1100px)` Regel, die den Button auf Modulseiten verbarg
- Der Back-to-Top-Button ist jetzt auf allen Seiten und Bildschirmgrössen sichtbar

---

## Kurzfazit

Alle 11 Commits sind **nicht-funktionale** UX/CSS-Verbesserungen und CI-Kalibrierungen. Keine Breaking Changes. Kein Einfluss auf Semantik, Barrierefreiheit-Grundstruktur oder Seitenstruktur. Die mobilen Navigations- und Notfall-Pfade bleiben unverändert.
